package ColumnBasedGame;

import java.util.ArrayList;

import javafx.concurrent.Service;
import javafx.concurrent.Task;
import javafx.scene.shape.Circle;

public class CheckTask extends Task<Boolean>{
	private final int col, row;
	private final boolean team;
	private final boolean[][] teams;
	
	public CheckTask(int col, int row, boolean team){
		this.col = col;
		this.row = row;
		this.team = team;
		teams = new boolean[ColumnGame.getWidth()][ColumnGame.getHeight()];
		for(GameColumn gc : ColumnGame.getColumns()){
			try{
				for(int i = 0)
			}
		}
	}
	
	
	@Override protected Boolean call() throws Exception {
		return team;
       
    }

	

}
